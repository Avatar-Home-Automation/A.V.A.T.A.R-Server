'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

const _ = require('underscore');

var _nlp_compromise = require('compromise');

var _nlp_compromise2 = _interopRequireDefault(_nlp_compromise);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (sentence, rules) {
  if (!Array.isArray(rules)) rules = [rules];
  var match = void 0;

  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = rules[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var rule = _step.value;
      var matches = _nlp_compromise2.default(sentence).compute('root').match(rule).terms().out('array');

      if (matches.length > 0 && matches[0] !== null) {

        matches = _nlp_compromise2.default(matches[0]).json()

        var values = {};
        var _iteratorNormalCompletion2 = true;
        var _didIteratorError2 = false;
        var _iteratorError2 = undefined;

        try {
          for (var _iterator2 = matches[0].terms[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
            var term = _step2.value;
            var tag = _.last(term.tags)
            var key = tag.toLowerCase();
            var text = key !== 'symbol' ? _nlp_compromise2.default(term.text).text() : term.text;
            if (!values[key]) {
              values[key] = text;
            } else {
              if (!Array.isArray(values[key])) values[key] = [values[key]];
              if (values[key].indexOf(text) === -1) {
                values[key].push(text);
              }
            }
          }
        } catch (err) {
          _didIteratorError2 = true;
          _iteratorError2 = err || 'NLP syntax Error';
        } finally {
          try {
            if (!_iteratorNormalCompletion2 && _iterator2.return) {
              _iterator2.return();
            }
          } finally {
            if (_didIteratorError2) {
              throw _iteratorError2;
            }
          }
        }
        match = Object.keys(values).length > 0 ? values : matches[0].text();
        break;
      }
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err || 'NLP syntax Error';
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator.return) {
        _iterator.return();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  return match;
};
