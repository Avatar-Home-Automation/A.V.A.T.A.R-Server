/**
 * This function is called by the A.V.A.T.A.R client when the dialog is done and
 * only if the speak() and play() functions are subclassed.
 * Allows you to perform an action, such as putting back the sound of a device after the dialogue.
 * @param {string} clientFrom - the A.V.A.T.A.R client which sent the rule
 */
export async function unmuteClosure (clientFrom) {

    // Do stuff

}

