/**
 * Executed on loading the plugin
 * To subclass the Avatar speak function by another one
 @param { none } 
*/
export async function subclassSpeak() {
	
  let defaultSpeak = Avatar.speak; // Backup the default Avatar speak function
  
	// Do stuff
  
}

